# Pulse 04: SPECIFICATION_BASELINE

Settled. Authored `docs/vtrace/SPECIFICATION_BASELINE.md` (target/provisional): the
**scale model** (SCALE-01..03: international/national/regional/local with
within-scale interpretation), the water dimension pool `DIM-01..13`, the hydrologic
basis (SPEC-HB-01/02: firm yield vs average), the **T1–T4 conveyance tier model**
(Interbasin/Transboundary → Regional → Local Distribution → Service) with
supply/quality/continuity/access SLA terms, controlled specs `SPEC-001..013`,
public contracts `IF-001..004`, and the spec gate (`pass_with_risk`).

Role-review fixed point (real `.roles`): one finding applied — supply (DIM-01)
risked reading as rights-free, resolved by adding the compact/treaty-compliance
term to the T1 SLA and keeping rights in SPEC-007. Pool weights, SLA thresholds,
and scale nesting (DEF-005) explicitly provisional.

Validation: `proof check .`, `git diff --check`. Next: TRACE.
